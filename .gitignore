1_data_extraction/data/
*.npy
patches/
2_data_preparation/*.csv
3_pre_processing/lunar_patches/
3_pre_processing/lunar_patches_alltiles/
3_pre_processing/lunar_patches_alltiles_256ppd/
data_preparation/
__pycache__/

# runtime artifacts - models, experiment tracking, logs
*.keras
mlruns/
*.db
*.log
log_*.txt

# per-run artifacts - superseded by the combined history and MLflow
4_training/checkpoints/*_params.json
4_training/checkpoints/history_*.csv

# patch data is ignored, but the evaluation match arrays are kept so a clone
# carries everything compare_models.py needs for one run
*.npz
!**/results/**/arrays.npz